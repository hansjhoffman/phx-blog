<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :title %>
    <%= text_input f, :title, class: "form-control", placeholder: "Elixir rocks!"%>
    <%= error_tag f, :title %>
  </div>

  <div class="form-group">
    <%= label f, :content %>
    <%= textarea f, :content, class: "form-control", rows: "10" %>
    <%= error_tag f, :content %>
  </div>

  <div class="form-group">
    <%= label f, :excerpt %>
    <%= textarea f, :excerpt, class: "form-control", rows: "4" %>
    <%= error_tag f, :excerpt %>
  </div>

  <div class="form-actions">
    <%= submit "Save", class: "btn btn--primary" %>
    <%= link "Cancel", to: Routes.admin_post_path(@conn, :index), class: "btn btn--secondary" %>
  </div>
<% end %>
